from django.apps import AppConfig

class PocAppsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'poc_apps'
    verbose_name = 'Applications de Démonstration (PoC)'
